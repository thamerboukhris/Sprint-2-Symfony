<form method="post" action="{{ path('app_services_delete', {'idService': service.idService}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.idService) }}">
    <button  class="btn btn-outline-danger" class="btn">Supprimer</button>
</form>
